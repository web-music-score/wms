{
    "name": "music-score-project",
    "version": "1.0.0",
    "author": "PahkaSoft",
    "license": "MIT",
    "private": true,
    "workspaces": [
        "./packages/*",
        "./apps/*"
    ],
    "scripts": {
        "build:lib": "npm run build -w packages/web-music-score",
        "build:dev (all workspaces)": "npm run build:lib && npm run build:dev --workspaces --if-present",
        "build:prod (all workspaces)": "npm run build:lib && npm run build:prod --workspaces --if-present"
    }
}